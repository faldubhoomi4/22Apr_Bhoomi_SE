#include<stdio.h>
main()
{
	int area, base, height;
	printf("Enter number value base:");
	scanf("%d",&base);
	printf("Enter number value height:");
	scanf("%d",&height);
	area=base*height/2;
	printf("Triangle area is: %d", area);
    	
}
