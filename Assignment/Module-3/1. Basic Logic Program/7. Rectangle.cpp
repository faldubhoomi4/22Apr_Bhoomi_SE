#include<stdio.h>
main()
{
	int area, width, length;
	printf("Enter number value Width:");
	scanf("%d",&weight);
	printf("Enter number value Length:");
	scanf("%d",&length);
	area=width*length;
	printf("triangle area is: %d", area);
    	
}
